Source: https://mariadb.com/docs/server/server-management/variables-and-modes/new_mode

NEW_MODE | Server | MariaDB Documentation
Webinar | Modernizing High-Volume Payment Systems: Real-Time Architecture and Demo
Watch Now
⌘
Ctrl
k
Download MariaDB
Contact Us
More
MariaDB Server Documentation
Quickstart Guides
Server Usage
Server Management
Deployment
Starting & Stopping
Automated Deployment & Administration
Server Monitoring & Logs
Variables and Modes
Options, System & Status Variables
Server Status Variables
Server System Variables
NEW_MODE
OLD_MODE
SQL_MODE
SQL_MODE=MSSQL
SQL_MODE=ORACLE
Security
Architecture
Clients & Utilities
HA & Performance
Reference
Powered by GitBook
On this page
For the complete documentation index, see
llms.txt
. This page is also available as
Markdown
.
ChatGPT
On this page
NEW_MODE
Explains the `NEW_MODE` system variable, introduced in MariaDB 11.4, which allows users to opt-in to new, potentially incompatible behaviors or optimizations that will become default in future version
The
NEW_MODE
system variable and command line switch were introduced in
MariaDB 11.4
to provide a way to enable new behavior in otherwise stable versions. Specifying a flag in the
NEW_MODE
variable enables the corresponding new behavior; otherwise, the old (stable) behavior is used. This can be useful to preserve execution plans in stable versions that may change when the new behavior is active.
A sample usage scenario is:
a fix (new behavior) is pushed into stable releases (for example, MariaDB 11.4 and MariaDB 11.8). It becomes available in
NEW_MODE
but isn't enabled by default.
in MariaDB 12.1, the fix (new behavior) is enabled without the switch. There's no way to turn it off.
NEW_MODE
does not list the fix as something that can be turned on.
if you specify
fix_X
that is no longer switchable, a warning is printed. However, if you specify
fix_that_never_existed
, an error is produced.
Syntax
You can set
NEW_MODE
from the
command line
using the
--new-mode
option, or by setting the
new_mode
system variable.
Copy
SET [GLOBAL|SESSION] new_mode = 'fix_1[,fix_2]...';
The session value only affects the current client and can be changed by the client when required. Setting the global value requires the SUPER privilege, and the change will affect any clients that connect from that point forward.
Example
Copy
SET
GLOBAL
new_mode
=
'
FIX_DISK_TMPTABLE_COSTS
'
;
...
SET
new_mode
=
'
FIX_DISK_TMPTABLE_COSTS,FIX_INDEX_STATS_FOR_ALL_NULLS
'
;
...
SET
SESSION
new_mode
=
'
FIX_INDEX_STATS_FOR_ALL_NULLS
'
;
SET
new_mode
=
''
;
Available NEW_MODE Flags
FIX_DISK_TMPTABLE_COSTS
FIX_INDEX_STATS_FOR_ALL_NULLS
FIX_DISK_TMPTABLE_COSTS
From
MariaDB 11.8.4
to
MariaDB 12.0
. Starting from
MariaDB 12.1.2
, this behavior is enabled by default.
This flag improves the cost computation for using temporary tables in certain cases, including semi-join subquery materialization (
MDEV-37723
).
FIX_INDEX_STATS_FOR_ALL_NULLS
From
MariaDB 11.4.9
to
MariaDB 12.0
.
This flag improves the selection of execution plans when indexed columns contain only NULL values (
MDEV-36761
). Starting from
MariaDB 12.1.2
, this behavior is enabled by default.
For proper application of the fix,
engine-independent statistics
must be collected for tables having columns with only NULL values:
or at least for indexed columns with only NULL values:
Previous
Server System Variables
Next
OLD_MODE
Last updated
22 days ago
Was this helpful?
Syntax
Example
Available NEW_MODE Flags
FIX_DISK_TMPTABLE_COSTS
FIX_INDEX_STATS_FOR_ALL_NULLS
Was this helpful?
Copy
ANALYZE TABLE table_name PERSISTENT FOR ALL;
Copy
ANALYZE TABLE table_name PERSISTENT FOR COLUMNS (b) INDEXES (key_b);